services:
  mcp-sentinel:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mcp-sentinel-cpp
    ports:
      - "8080:8080"
    environment:
      MCP_SENTINEL_PORT: 8080
      MCP_SENTINEL_AUDIT_LOG_PATH: /app/logs/audit.log
    volumes:
      - ./logs:/app/logs